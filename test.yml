---
- name: Testing AAP Project Creation
  hosts: controller
  become: true
  tasks:

    - name: Display test message
      ansible.builtin.debug:
        msg: "This is a test from {{ inventory_hostname }}!"
